import { SearchRidePipe } from './search-ride.pipe';

describe('SearchRidePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRidePipe();
    expect(pipe).toBeTruthy();
  });
});
